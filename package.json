{
  "name": "memory-graph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "npx serve . -p 3000",
    "test:e2e": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "serve": "^14.0.0"
  }
}
